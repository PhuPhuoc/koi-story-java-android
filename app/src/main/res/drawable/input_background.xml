<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền của khung -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Màu viền -->
    <corners android:radius="0dp" /> <!-- Bo góc cho viền -->
    <padding
        android:bottom="8dp"
        android:left="8dp"
        android:right="8dp"
        android:top="8dp" />
</shape>